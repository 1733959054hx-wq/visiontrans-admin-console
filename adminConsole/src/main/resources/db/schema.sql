-- =============================================================================
--  本文件仅作「参考建表脚本」，项目正常运行不需要执行它。
--  实际表结构由 JPA（spring.jpa.hibernate.ddl-auto=update）在应用启动时自动创建/更新；
--  建库与账号授权请执行 db/init.sql（位于 adminConsole/db/init.sql）。
-- =============================================================================
SELECT 'schema.sql 仅作参考，建表由 JPA 自动完成' AS note;
