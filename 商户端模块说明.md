# 视界译 VisionTrans · 商户端模块补充说明（jingchen）

> 本仓库基于团队基础代码，新增 **商户端（jingchen 模块）六大业务模块**。
> 全部代码位于 `jingchen` 包 / 目录内，未改动任何基础工程文件（仅扩展模块自有文件）。

## 模块清单（工作台首页六卡，登录商户可见）

| 模块 | 后端表（启动自动建 + 灌种子） | 接口（前缀 /api/merchant） | 页面 |
|---|---|---|---|
| 经营概览 | `merchant_daily_stat`（14 天日统计） | `GET /overview` | 指标卡 + 近 7 日趋势 + 订单摘要 |
| 投放计划管理 | `ad_plan`（4 条） | `GET/POST/PUT/DELETE /plans`、`PUT /plans/{id}/pause\|resume` | 列表 + 新建/编辑/暂停恢复/删除 |
| 订单与结算 | `merchant_order`（8 笔） | `GET /orders`、`PUT /orders/{id}/settle` | 列表 + 一键结算流转 |
| 素材管理 | `merchant_material`（4 条）、`merchant_ab_config` | `GET/POST/PUT/DELETE /materials`、`GET/PUT /materials/ab` | 素材 CRUD + A/B 分流配置 |
| 视频接入 | `merchant_video`（6 部） | `GET/POST/PUT/DELETE /videos` | 上传档案 / 元数据 / 字幕 / 播放数据 |
| 推广与销售 | `merchant_channel`（4 个） | `GET/POST/PUT/DELETE /promotions`、`GET /sales` | 渠道 CRUD + 二维码短链预览 + 销售报表 |

## 启动步骤

1. **数据库**：MySQL 8，库名 `shixun`（首次用 root 执行 `adminConsole/db/shixun.sql` 或等价初始化，含账号授权）
2. **后端**：JDK 21 + Maven，环境变量：
   ```
   ADMIN_DB_USER=shixun
   ADMIN_DB_PASSWORD=123456
   ADMIN_DDL_AUTO=update        # 首次自动建表
   ```
   启动 `adminConsole` 主类（`mvn spring-boot:run`）。日志出现各 `[商户端初始化]` 行即种子灌入成功。
3. **前端**：`admin-front` 下 `npm install` → `npm run dev`，访问 http://localhost:5173

## 账号

| 身份 | 账号 | 口令 |
|---|---|---|
| 商户 | `merchant` | `merchant123`（初始化器按 `admin-console.jingchen.username/password` 自动创建） |
| 管理员 | `admin` | `admin123` |

登录页切换「商户」身份进入商户工作台。

## 约定（与基础工程一致）

- 鉴权：`X-Auth-Token`；商户走独立 `merchant_account` 会话表，接口类级 `@RequireRole(MERCHANT)`，与后台管理双向隔离（管理员令牌调商户接口返回 403）
- 分层：entity → repository(EntityManager+JPQL) → service → controller；前端 view → store → api
- 前端复用通用组件：`CrudDialog` / `ConfirmDialog` / `StatusPill` / `PageHeader` 等
- 演示数据初始化器均「表空才灌、不覆盖」，重置演示数据请先清表再重启

## 2026-09-11 迭代：真实文件上传 / 字幕独立表 / 视频播放统计

| 能力 | 后端 | 前端 | 说明 |
|---|---|---|---|
| 真实文件上传 | `POST /merchant/files`（multipart，kind=image/video/subtitle/doc） + `GET /merchant/files/{storedName}` 取回 | `MerchantFileDrop`（拖拽/预览/进度） + `MerchantUploadDialog`（上传建档） | 类型白名单 + 分级限额（图片20MB/视频500MB/字幕5MB/资质20MB）；素材、视频、入驻资质三处均真实落盘，名称/大小/形态由服务端按落盘文件回填；取回需商户令牌（管理员 403） |
| 字幕独立管理 | `merchant_subtitle` 表；`GET/POST/PUT/DELETE /merchant/videos/{videoId}/subtitles` | 视频页每行「字幕」按钮 → 字幕面板（列表 + 按语种添加 + 删除 + 下载） | 同视频同语种唯一；`merchant_video.subtitle_langs` 由增删自动回写，仅作展示 |
| 视频播放统计 | `merchant_video_daily` 表（视频×日，种子近 14 天）；`GET /merchant/videos/stats` | `/merchant/video-stats`（ECharts：趋势折线+完播率右轴、地域环形、单视频 TOP 对比、完播率明细） | 完播率一律按播放量加权；入口在「视频接入」页头「播放统计」按钮 |

配套：模块自有组件 `MerchantChart.vue`（折线/柱状/饼按需注册）、`composables/useFileUrl.js`（鉴权 blob→objectURL 预览/下载）；迁移脚本 `adminConsole/db/upgrade-merchant-files-subtitles-stats.sql`（幂等）；快照 `adminConsole/db/shixun.sql` 已同步（52 张表）。上传文件存 `admin-console.jingchen.upload-dir`（默认 `uploads/merchant`，uuid 重命名），删除档案不删落盘文件。

## 2026-09-11 迭代二：商户导航抽屉 / 返回键回登录页 / 免验证码宽限

| 能力 | 实现 | 说明 |
|---|---|---|
| 商户导航抽屉 | `jingchen/components/MerchantNavDrawer.vue` + TopBar 汉堡按钮接入 | 商户身份下点顶栏汉堡从左滑出导航（11 个页面 + 当前页高亮 + 点击跳转），任意页面可互相切换；管理员身份仍是原侧栏折叠 |
| 返回键回登录页 | 登录成功跳转 `replace` → `push`；登录页加「已登录 · 返回工作台」横幅；路由守卫放行已登录用户访问 /login | 工作台按浏览器「返回」回到登录页（不再退出站点），横幅可一键返回工作台 |
| 登录免验证码宽限 | `common/CaptchaGraceService`（内存态，重启即清）；管理员/商户登录接入 | 成功登录后 10 分钟内同一账号重登免验证码；登录页两段式提交：点「登 录」先直连提交，后端要求验证码时按业务码 460 自动弹出安全挑战面板；登录身份按 `admin_console_identity` 预选 |
